# Export policy JSON files for OCI image packaging.
# The gcp/BUILD file references these via pkg_tar to bundle
# policies into the container image at /policies/.

package(default_visibility = ["//visibility:public"])

exports_files(["policy.json"])
